# Local-only tooling: reading data/targets_raw.xlsx and building the tracker.
# Not installed on the scraping server -- these wheels lag new Python releases
# and would otherwise force a source build there.
#
#   pip install -r requirements.txt -r requirements-dev.txt

pandas==2.2.3
openpyxl==3.1.5
